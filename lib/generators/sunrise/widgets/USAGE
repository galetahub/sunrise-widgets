Usage:
  rails generate sunrise:widgets:install [options]

Options:
  [--migrations]  # Indicates when to generate migrations
                  # Default: true
                 
Example:

  rails g sunrise:widgets:install
  
  rails g sunrise:widgets:install --skip-migrations
